package hu.schonherz.y2014.partyappandroid.dialogs;

import android.support.v4.app.DialogFragment;

public class HighlightExpireDialog extends DialogFragment {
    /*
     * @Override public Dialog onCreateDialog(Bundle savedInstanceState) {
     * AlertDialog.Builder builder = new AlertDialog.Builder(getActivity());
     * LayoutInflater inflate =
     * LayoutInflater.from(ClubActivity.activityForDialog); View v =
     * inflate.inflate(R.layout.highlight_expire_dialog, (ViewGroup)
     * ClubActivity.activityForDialog.findViewById(R.id.club_activity_root));
     * builder.setView(v); Dialog d = builder.create();
     * d.setCanceledOnTouchOutside(false); return d; }
     */
}
